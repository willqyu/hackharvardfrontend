import React from "react";
import { Button } from '@/components/ui/button';


const reportProps = {
    image_data: String,
    comment: String
}

const SubmitReport = (

) => {
    return (
        <Button onClick={() => {}}>
                    Submit Photo
        </Button>
    )
}

export default SubmitReport;
